services:
  qdrant:
    image: qdrant/qdrant:v1.13.4
    container_name: agent-code-analyzer-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
